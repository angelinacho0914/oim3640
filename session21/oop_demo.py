# Type: Point, represent a point in 2-D space
# Ways we represent points:
    # Store the coordinates separtely in two variables, x and y.
    # Store the coordinates as elements in a list or tuple.
    # Create a new type to represent points as objects.